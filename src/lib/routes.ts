/**
 * Adresses structurantes du site, définies à un seul endroit.
 *
 * La racine est publique : c'est la page que voient les membres. L'espace du
 * bureau commence au tableau de bord.
 */

/** Accueil public : les actualités du comité. */
export const PUBLIC_HOME = "/";

/** Second onglet public. */
export const PUBLIC_VISION = "/vision";

/** Page d'arrivée après connexion, pour un trésorier. */
export const HOME_AFTER_LOGIN = "/tableau-de-bord";

/** Page d'arrivée après connexion, pour le profil « communication ». */
export const HOME_COMMUNICATION = "/annonces";
